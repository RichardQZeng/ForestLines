version: "3"
services:
  qgis:
    build:
      context: .
    volumes:
      - ./:/storage
    tty: true
    # run a python file that calls all of the tests
    command: python3 storage/geo_trace/test/test_init.py
